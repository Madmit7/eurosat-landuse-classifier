defaults:
  - _self_
  - data: eurosat
  - model: resnet18
  - trainer: default
  - logger: mlflow
  - export

seed: 42

train:
  batch_size: 64
  lr: 0.0003
  weight_decay: 0.0001

artifacts_dir: artifacts
plots_dir: plots

infer:
  checkpoint_path: artifacts/best.ckpt
